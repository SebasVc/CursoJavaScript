"use strict"
/*
variable.length -->arroja el numero de caracteres de la cadena de texto
variable.indexOf("palabra o letra") --> Arroja la posición de la palabra o letra que buscas, tambien sirve el metodo search("palabra o letra")
variable.replace("palabra a reemplazar","reemoplazo"); --> busca y reemplaza una palabra o frase de una cadena 
variable.slice("posicion desde, hasta(no es obligatori)) --> recorta el string desde una posición indicada
variable.split ("")--> Crea un array de la cadena indicando un separador previamente o sin indicar crea cadena de caracter x caracter
variable.trim()-->Quita espacios por delante o por detroas de un string i

/// arrays
arreglo.join()---> convierte el array en una cadena de string
arreglo.pop()--> elimina  el ultimo elemento del arreglo
arreglo.push(elemento)-->Añade un elemento al arreglo

